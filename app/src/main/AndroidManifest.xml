<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.vntvisi.diceroller">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_dice"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_dice_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DiceRoller">
        <activity
            android:name=".desafio.dia20210806.DesafioRotacaoTelaActivity"
            android:exported="true" />
        <activity
            android:name=".desafio.dia20210804.DesafioFragmentActivity"
            android:exported="true" />
        <activity android:name=".desafio.dia20210730.DesafioIntentActivity" />
        <activity android:name=".About" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>